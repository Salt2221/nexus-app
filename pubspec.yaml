name: nexus
description: Nexus - multi-platform messenger with VPN
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.3.6
  path_provider: ^2.1.5
  url_launcher: ^6.3.2
  http: ^1.6.0
  provider: ^6.1.5
  nested: ^1.0.0
  webview_flutter: ^4.13.1
  web_socket_channel: ^3.0.3
  open_filex: ^4.7.0
  dio: ^5.9.2
  crypto: ^3.0.7
  qr_flutter: ^4.1.0
  telegram_client: ^1.0.0
  socks5_proxy: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
